# Devlog – FastAPI Bookstore

## 2025-08-06

**What I did:**
- Integrated Alembic for database migrations.
- Replaced direct table creation with Alembic-managed schema.
- Generated and applied the initial migration for the `books` table.
- Cleaned up `env.py` to use app's SQLAlchemy engine.
- Added basic Book model, schema, and CRUD.
- Ran FastAPI app locally with Uvicorn.
- Manually tested `/books/` POST and GET endpoints via Postman.
- Inserted 7 books (JSON payloads) to verify DB insert logic and validation.

**What I learned:**
- Alembic gives better control over schema changes than raw `Base.metadata.create_all()`.

**Next steps:**
- Figure out how everything actually works and connects :)
- Expand the project further.
