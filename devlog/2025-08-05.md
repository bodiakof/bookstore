# Devlog – FastAPI Bookstore

## 2025-08-05

**What I did:**
- Set up the basic project structure with empty placeholder files.
- Created a script that generates dated log entry templates in the 'devlog' forlder.

**Next steps:**
- Build the Book model, schema, CRUD logic, and routes.
- Connect to a SQLite database using SQLAlchemy.
- Add validation to the book schema (e.g., no negative price).
- Learn Alembic to handle database migrations.
