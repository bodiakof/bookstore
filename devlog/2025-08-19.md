# Devlog – FastAPI Bookstore

## 2025-08-19

**What I did:**
- Added a `Dockerfile` for the FastAPI app with Python dependencies and auto-reload for development.

- Created `docker-compose.yaml` to spin up FastAPI, Postgres, Grafana, Loki, and Promtail with proper volumes and ports.

- Added `promtail-config.yaml` to scrape FastAPI logs and push them to Loki.

- Restored Postgres database from a backup file.

- Changed `Union` to `|` operator in `app/api/book.py`.
<br>
<br>

**What I learned:**
- Mounting the app folder as a volume allows code changes in VSCode to reflect immediately inside the container.

- Using `.env` files is cleaner than hardcoding environment variables in `docker-compose.yaml`.
<br>
<br>

**Next steps:**
- Start experimenting with Grafana dashboards to visualize FastAPI logs.

- Add `Authors` table.
