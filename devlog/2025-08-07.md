# Devlog – FastAPI Bookstore

## 2025-08-07

**What I did:**
- Created a basic Python CLI client using `argparse` and `httpx`.
- Implemented `add` and `list` commands to interact with `/books/` endpoint.
- Verified integration by adding books and listing them from the command line.

**What I learned:**
- CLI clients can communicate with FastAPI endpoints the same way as Postman.

**Next steps:**
- Expand `crud.py` to support full CRUD operations (get by ID, update, delete).
- Add missing FastAPI routes (`GET /books/{id}`, `PUT /books/{id}`, `DELETE /books/{id}`).
- Update the CLI to include `get`, `update`, and `delete` commands.
