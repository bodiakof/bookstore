# Devlog – FastAPI Bookstore

## 2025-08-15

**What I did:**
- Implemented full CRUD operations for books (list, create, read, update, delete).
- Modified `create_book` to support creating both single and multiple books at once.
- Ensured PATCH and PUT routes can update books properly, with partial updates supported via `exclude_unset=True`.
- Added new genres to Genre enum, updated alembic's revisions to support additions.
<br>
<br>

**Bugs encountered:**
- Duplicate entries get created in the database.
<br>
<br>

**Next steps:**
- Figure out how to prevent duplicates in the db.
- Possibly add unique constraints or validation checks before creating new books.
